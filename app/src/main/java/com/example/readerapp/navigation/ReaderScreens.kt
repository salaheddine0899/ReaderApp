package com.example.readerapp.navigation

enum class ReaderScreens(val path: String) {
    SPLASH_SCREEN(path = "splash"),
    HOME_SCREEN(path = "home"),
    LOGIN_SCREEN(path = "login"),
    SIGNUP_SCREEN(path = "signup");

}